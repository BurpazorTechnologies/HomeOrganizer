import 'package:home_organizer_flutter_app/config/app_config.dart';

/// Production environment.
///
/// Update the URL to match your production API host.
const AppConfig prodConfig = AppConfig(
  environment: AppEnvironment.prod,
  apiBaseUrl: 'https://homeorganizer.xyz',
);


