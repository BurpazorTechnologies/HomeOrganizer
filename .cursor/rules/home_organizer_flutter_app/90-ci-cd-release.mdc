---
description: |
  CI/CD and release discipline: required checks, build steps, signing, and release hygiene.
globs:
  - "home_organizer_flutter_app/lib/**/*.dart"
  - "home_organizer_flutter_app/test/**/*.dart"
  - "home_organizer_flutter_app/pubspec.yaml"
  - "home_organizer_flutter_app/analysis_options.yaml"
alwaysApply: true
---
# CI/CD & Release

## Required CI checks
- ✅ `dart format --set-exit-if-changed .`
- ✅ `dart analyze`
- ✅ `flutter test`
- ✅ Build per entrypoint (at least dev + prod):
  - `flutter build <target> -t lib/main_dev.dart`
  - `flutter build <target> -t lib/main_prod.dart`

## Release hygiene
- ✅ Release checklist includes:
  - version bump
  - changelog update
  - smoke test critical flows
  - verify crash reporting enabled
  - verify env points to correct backend

## Signing
- ✅ Keep signing keys in CI secrets or secure vault
- ✅ Never store keystores in repo

## NEVER
- ❌ Merge code that fails analyze/tests
- ❌ Ship without confirming correct flavor configuration
